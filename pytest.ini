[pytest]
testpaths = tests
# Scrapling's lxml usage triggers a strip_cdata DeprecationWarning that is not
# ours to fix; keep the suite's warning output meaningful.
filterwarnings =
    ignore:.*strip_cdata.*:DeprecationWarning
    ignore:Using `httpx` with `starlette.testclient` is deprecated.*
